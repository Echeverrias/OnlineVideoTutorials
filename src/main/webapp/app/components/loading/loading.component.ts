import { Component } from '@angular/core';

import { loadingTemplate } from './loading.html';

@Component({
    moduleId: module.id,
    selector: "ovt-loading",
    styleUrls: ["loading.css"],
    template: loadingTemplate
})
export class LoadingComponent{

}